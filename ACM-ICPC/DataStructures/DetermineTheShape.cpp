#include<bits/stdc++.h>
#define ii pair<int, int> 
using namespace std;

struct point {
	int x, y;
};

#define sq(a) ((a)*(a))
#define dist(p,q) (sq(p.x-p.x)+sq(p.y-q.y))

int main() {


	int T;
	cin >> T;
  	point p1, p2, p3, p4;
	while(T--) {
		cin >> p1
	}

	return 0;
}